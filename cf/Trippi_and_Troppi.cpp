#include <bits/stdc++.h>
using namespace std;

int main() {
    int tests;
    cin >> tests;
    while (tests--) {
        string A, B, C;
        cin >> A >> B >> C;
        cout << A.front() << B.front() << C.front() << '\n';
    }
    return 0;
}
